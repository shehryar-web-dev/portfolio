/**
 * Intentionally empty.
 *
 * The previous version rendered animated glow orbs behind the page. The
 * redesign uses a static hairline grid inside the hero instead, so there is no
 * full-page decorative layer to paint on every route.
 */
export function Background() {
  return null;
}
